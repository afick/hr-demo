export const DEFAULT_DATE_FORMAT = 'YYYY-MM-DD'
export const hasStringValue = (input: string | null | undefined) => input !== null && input !== undefined && input.length > 0 && input.trim().length > 0
